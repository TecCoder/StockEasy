import { useResource } from '../hooks/useResource';
import { ErrorNotice, Loading, PageHeader } from '../components/UI';
type Status = { provider: string; configured: boolean; requests_today: number; counter_date: string | null; failures: number; cache_entries: number; last_success: string | null; retry_after: string | null; limits: { day: number; minute: number; month: number } };
export function Providers() { const state = useResource<Status[]>('/providers'); return <><PageHeader eyebrow="OBSERVABILIDAD" title="Estado de los datos"><button onClick={state.reload}>Actualizar estado</button></PageHeader><ErrorNotice error={state.error} /><section className="panel">{state.loading ? <Loading /> : <div className="table-wrap"><table><thead><tr><th>PROVEEDOR</th><th>ESTADO</th><th>PETICIONES / CUOTA DIARIA</th><th>CACHÉ</th><th>ÚLTIMO ÉXITO</th><th>FALLOS</th></tr></thead><tbody>{state.data?.map(p => <tr key={p.provider}><td><strong>{p.provider}</strong></td><td>{!p.configured ? 'Falta configuración' : p.retry_after && new Date(p.retry_after + (p.retry_after.endsWith('Z') ? '' : 'Z')) > new Date() ? 'En espera' : 'Configurado'}</td><td>{p.requests_today} / {p.limits.day}<small>{p.counter_date ?? 'Sin peticiones'} · {p.limits.minute}/min</small></td><td>{p.cache_entries}</td><td>{p.last_success ? new Date(p.last_success).toLocaleString() : 'Sin conexión verificada'}</td><td>{p.failures}</td></tr>)}</tbody></table></div>}</section><p className="muted">Las cuotas mostradas son límites conservadores de esta aplicación. El acceso y la cobertura dependen de tu plan. Configura las claves en .env.</p><a href="https://www.coingecko.com/" target="_blank" rel="noreferrer">Crypto data powered by CoinGecko</a></>; }
